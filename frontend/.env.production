# Leave empty so the browser calls same-origin `/api/*` (proxied by Vercel → Hugging Face).
# For local production builds against a live API instead, set e.g.:
# VITE_API_BASE=https://feynmanproject-myremover-api.hf.space/api
VITE_API_BASE=
